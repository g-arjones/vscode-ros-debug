describe("ConfigurationProvider", () => {
    beforeEach(() => {
        // no-op
    });
    afterEach(() => {
        // no-op
    });
    describe("resolveDebugConfiguration", () => {
        it("returns an unchanged configuration", async () => {
            // no-op
        });
    });
});
